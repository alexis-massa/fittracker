pre-commit:
  parallel: true
  commands:

    ruff-format:
      glob: "*.py"
      run: ruff format {staged_files}

    ruff-lint:
      glob: "*.py"
      run: ruff check --fix {staged_files}

    mypy:
      run: mypy src